---
import { cn } from "@/lib/utils";

const { className, gap } = Astro.props;

const defaultClass = `flex-col space-y-${gap ?? 5} h-full`;
---

<div class={cn(defaultClass, className)}>
  <slot />
</div>
